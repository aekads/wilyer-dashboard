{
  "name": "aekads",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && node src/server.js",
    "build": "cd frontend && NODE_ENV=development npm install && npm run build",
    "render-build": "npm run build",
    "postinstall": "cd backend && npm install --omit=dev"
  }
}